# ダッシュボード（別プロセス）専用の依存。
# bot 本体の requirements.txt とは分離する（discord.py・matplotlib 等は不要）。
#
# インストール:
#   cd club-bot
#   venv/bin/pip install -r dashboard/requirements.txt
#
# DB ドライバ（asyncpg / aiosqlite）と cryptography は bot 本体の
# requirements.txt に含まれる。ダッシュボードだけを別 venv で動かす場合は
# 下のコメントを外すこと。
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
# 署名付きセッション Cookie（starlette の SessionMiddleware が使用）
itsdangerous>=2.2.0
# Discord OAuth2 のトークン交換・API 呼び出し
httpx>=0.27.0

# ダッシュボードを bot と別 venv で動かす場合に必要:
# asyncpg>=0.29.0
# aiosqlite>=0.19.0

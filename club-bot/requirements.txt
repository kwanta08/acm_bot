discord.py>=2.3.0,<3.0.0
python-dotenv>=1.0.0
todoist-api-python>=2.1.0
aiosqlite>=0.19.0
cryptography>=42.0.0
asyncpg>=0.29.0

# 注: Google Sheets 連携は撤去しました。
#     CSV 出力は Web ダッシュボード（dashboard/）と /report export-tasks が担います。
#     旧シートからの一回限りの移行スクリプト
#     （scripts/migrate_sheets_to_db.py / scripts/migrate_progress_sheet_to_db.py）を
#     実行するときだけ、その場で次を追加インストールしてください:
#       pip install gspread google-auth
